# Campus Seats 📱

Приложение для выбора мест в аудитории с использованием интерактивной сетки, генерацией QR-кодов и сохранением выбора.

## 📋 Описание

**Campus Seats** — это iOS приложение, разработанное на SwiftUI, которое позволяет студентам выбирать места в аудитории для различных событий (лекций, семинаров, экзаменов). Приложение включает интерактивную сетку мест, генерацию QR-кодов для выбранных мест и автоматическое сохранение выбора.

## ✨ Основные функции

### 1. 📅 Список событий
- Отображение всех доступных событий
- Информация о каждом событии (название, дата, аудитория, количество мест)
- Pull-to-refresh для обновления списка
- Красивый UI с градиентами и анимациями

### 2. 🪑 Интерактивная сетка мест (Grid Layout)
- Динамическая генерация сетки мест на основе конфигурации события
- Визуальная индикация статуса мест:
  - 🟢 **Зеленый** — свободное место
  - 🔵 **Синий** — выбранное место
  - ⚫ **Серый** — занятое место
- Плавные анимации при выборе места
- Легенда для понимания цветов

### 3. 👆 Выбор места
- Тап по месту для выбора
- Автоматическое снятие предыдущего выбора
- Валидация: нельзя выбрать занятое место
- Визуальная обратная связь (анимации, тени)

### 4. 🔒 Отметка мест как занятых
- Долгое нажатие (Long Press) на место отмечает его как занятое
- Тактильная обратная связь (Haptic Feedback)
- Автоматическое снятие выбора при отметке как занятое

### 5. 📱 Генерация QR-кода
- Автоматическая генерация QR-кода для выбранного места
- QR-код содержит:
  - ID события
  - ID места
  - Timestamp выбора
- Красивый UI для отображения QR-кода
- Возможность показать QR-код на входе

### 6. 💾 Сохранение данных
- Автоматическое сохранение выбранного места в UserDefaults
- Восстановление выбора при перезапуске приложения
- Сохранение для каждого события отдельно

### 7. 🎨 Улучшенный UI/UX
- Современный дизайн с градиентами
- Плавные анимации и переходы
- Адаптивная верстка
- Поддержка темной темы iOS
- Тактильная обратная связь

### 8. ✅ Валидация и обработка ошибок
- Валидация конфигурации событий
- Проверка доступности мест
- Обработка ошибок загрузки данных
- Информативные сообщения об ошибках

## 🏗️ Архитектура

Приложение использует **MVVM (Model-View-ViewModel)** архитектуру с разделением на слои:

```
Campus Seats/
├── Models/              # Модели данных
│   ├── Seat.swift              # Модель места
│   ├── Event.swift             # Модель события
│   └── SeatSelection.swift     # Модель выбранного места
├── ViewModels/          # Бизнес-логика
│   ├── SeatGridViewModel.swift # Логика grid и выбора мест
│   └── EventViewModel.swift    # Логика событий
├── Views/               # UI компоненты
│   ├── EventsListView.swift    # Список событий
│   ├── EventCardView.swift     # Карточка события
│   ├── SeatView.swift          # Виджет отдельного места
│   ├── SeatGridView.swift      # Основной grid с местами
│   ├── EventInfoView.swift     # Информация о событии
│   └── QRCodeView.swift        # Отображение QR кода
├── Services/            # Сервисы
│   ├── StorageService.swift    # Сохранение данных (UserDefaults)
│   └── QRCodeService.swift     # Генерация QR кодов (CoreImage)
└── ContentView.swift           # Главный экран
```

## 🔧 Технологии

- **SwiftUI** — современный UI фреймворк
- **Swift 5** — язык программирования
- **MVVM** — архитектурный паттерн
- **CoreImage** — генерация QR-кодов
- **UserDefaults** — локальное хранилище
- **Combine** — реактивное программирование (через @Published)

## 📱 Требования

- iOS 17.0+
- Xcode 15.0+
- Swift 5.9+

## 🚀 Установка и запуск

1. Клонируйте репозиторий:
```bash
git clone https://github.com/yourusername/campus-seats.git
cd campus-seats
```

2. Откройте проект в Xcode:
```bash
open "Campus Seats.xcodeproj"
```

3. Выберите симулятор или устройство

4. Нажмите `Cmd + R` для запуска

## 📖 Использование

### Выбор места:
1. Откройте приложение
2. Выберите событие из списка
3. Тапните на свободное место в сетке
4. Место будет выделено синим цветом
5. Нажмите "Показать QR код" для просмотра QR-кода

### Отметка места как занятого:
1. Нажмите и удерживайте (Long Press) на месте
2. Место станет серым (занятым)
3. Занятые места нельзя выбрать

## 🎯 Grid Logic

### Генерация сетки:
- Сетка генерируется динамически на основе `totalRows` и `totalColumns` события
- Каждое место имеет уникальный ID: `"{row}_{column}"`
- Номер места отображается в формате: `"A1"`, `"B3"`, `"C5"` и т.д.

### Структура данных:
```swift
struct Seat {
    let id: String           // Уникальный идентификатор
    let row: Int             // Номер ряда (0-based)
    let column: Int          // Номер колонки (0-based)
    var isSelected: Bool     // Выбрано ли место
    var isOccupied: Bool     // Занято ли место
    var seatNumber: String   // Отображаемый номер (A1, B2...)
}
```

## 📸 Скриншоты

### Список событий
- Отображение всех доступных событий с информацией

### Grid мест
- Интерактивная сетка с цветовой индикацией
- Легенда для понимания статусов

### QR код
- Генерация и отображение QR-кода для выбранного места

## 🔐 QR Code Generation

### Формат данных QR-кода:
```
Event:{eventId}|Seat:{seatId}|Time:{timestamp}
```

### Процесс генерации:
1. Создание строки данных из выбранного места
2. Использование `CIFilter` с типом `CIQRCodeGenerator`
3. Масштабирование для лучшего качества
4. Конвертация в `UIImage`

### Пример:
```swift
let qrData = "Event:event_001|Seat:0_0|Time:1702034567.89"
let qrImage = QRCodeService.shared.generateQRCode(from: qrData)
```

## 💾 Data Model

### Event (Событие):
```swift
struct Event {
    let id: String
    let name: String
    let date: Date
    let room: String
    let totalRows: Int
    let totalColumns: Int
    var totalSeats: Int  // Вычисляемое свойство
}
```

### Seat (Место):
```swift
struct Seat: Identifiable {
    let id: String
    let row: Int
    let column: Int
    var isSelected: Bool
    var isOccupied: Bool
    var seatNumber: String  // A1, B2, C3...
}
```

### SeatSelection (Выбранное место):
```swift
struct SeatSelection {
    let eventId: String
    let seatId: String
    let seatNumber: String
    let timestamp: Date
    var qrData: String  // Данные для QR-кода
}
```

## 🎨 UI/UX Особенности

- **Градиенты**: Использование градиентов для фона и кнопок
- **Анимации**: Плавные spring-анимации при взаимодействии
- **Тени**: Многослойные тени для глубины
- **Цветовая схема**: Интуитивная цветовая индикация статусов
- **Тактильная обратная связь**: Haptic Feedback при долгом нажатии
- **Адаптивность**: Корректное отображение на разных размерах экрана

## 🧪 Тестирование

Для тестирования приложения:
1. Запустите приложение в симуляторе
2. Выберите событие
3. Попробуйте выбрать разные места
4. Отметьте места как занятые (Long Press)
5. Проверьте генерацию QR-кода
6. Перезапустите приложение и убедитесь, что выбор сохранился

## 📝 TODO / Будущие улучшения

- [ ] Добавить синхронизацию с сервером
- [ ] Реализовать авторизацию пользователей
- [ ] Добавить уведомления о событиях
- [ ] Реализовать историю выбора мест
- [ ] Добавить возможность отмены выбора
- [ ] Улучшить обработку ошибок с показом алертов
- [ ] Добавить unit-тесты
- [ ] Добавить widget-тесты

## 👨‍💻 Автор

Разработано для демонстрации навыков iOS разработки.

## 📄 Лицензия

Этот проект создан в образовательных целях.

---

**Приятного использования! 🎓**

